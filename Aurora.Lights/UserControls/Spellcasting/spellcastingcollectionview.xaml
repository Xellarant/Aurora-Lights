<UserControl x:Class="Builder.Presentation.UserControls.Spellcasting.SpellcastingCollectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Builder.Presentation.UserControls.Spellcasting">
  <FrameworkElement.DataContext>
    <local:SpellcastingCollectionViewViewModel/>
  </FrameworkElement.DataContext>
  <Grid>
    <DataGrid Style="{StaticResource ItemsDataGridStyle}" Margin="0" ItemsSource="{Binding SpellCollection}"
              SelectedItem="{Binding SelectedElement}">
      <DataGrid.ColumnHeaderStyle>
        <Style TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
          <Setter Property="Control.FontSize" Value="12"/>
          <Setter Property="Control.FontWeight" Value="SemiBold"/>
          <Setter Property="Control.Padding" Value="5,4,5,5"/>
          <Setter Property="Control.Foreground" Value="{DynamicResource IdealForegroundColorBrush}"/>
        </Style>
      </DataGrid.ColumnHeaderStyle>
      <DataGrid.Columns>
        <DataGridTextColumn x:Name="ItemNameColumn" Header="NAME" Width="2*"
                            CellStyle="{StaticResource ItemsDataGridCellStyle}" Binding="{Binding Name}"/>
        <DataGridTextColumn x:Name="ItemWeightColumn" Header="DESCRIPTION" Width="2*" MinWidth="200">
          <DataGridColumn.CellStyle>
            <Style TargetType="{x:Type DataGridCell}" BasedOn="{StaticResource ItemsDataGridCellStyle}">
              <Setter Property="ContentControl.ContentTemplate">
                <Setter.Value>
                  <DataTemplate>
                    <TextBlock FontStyle="Italic" HorizontalAlignment="Left" Text="{Binding Underline}"/>
                  </DataTemplate>
                </Setter.Value>
              </Setter>
            </Style>
          </DataGridColumn.CellStyle>
        </DataGridTextColumn>
        <DataGridTextColumn x:Name="ItemSourceColumn" Header="SOURCE" Width="Auto" MinWidth="100">
          <DataGridColumn.CellStyle>
            <Style TargetType="{x:Type DataGridCell}" BasedOn="{StaticResource ItemsDataGridCellStyle}">
              <Setter Property="ContentControl.ContentTemplate">
                <Setter.Value>
                  <DataTemplate>
                    <TextBlock HorizontalAlignment="Right" Margin="0,0,10,0" Text="{Binding Source}"/>
                  </DataTemplate>
                </Setter.Value>
              </Setter>
            </Style>
          </DataGridColumn.CellStyle>
        </DataGridTextColumn>
      </DataGrid.Columns>
    </DataGrid>
  </Grid>
</UserControl>