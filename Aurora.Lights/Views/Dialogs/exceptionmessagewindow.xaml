<controls:MetroWindow x:Class="Builder.Presentation.Views.Dialogs.ExceptionMessageWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:local="clr-namespace:Builder.Presentation.Views.Dialogs"
                      xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
                      Style="{StaticResource IconlessCharacterBuilderWindowStyle}" WindowStartupLocation="CenterScreen"
                      ResizeMode="CanMinimize"
                      TitleTemplate="{StaticResource IconlessCharacterBuilderWindowTitleDataTemplate}"
                      Title="Aurora Builder" MinHeight="350" MinWidth="600" Height="350" Width="600">
  <Grid Background="{DynamicResource WhiteBrush}" Margin="0,1,0,0">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <StackPanel>
      <TextBlock x:Name="txtTitle" Style="{StaticResource InformationDocumentTitleStyle}" Margin="10,10,10,0"/>
      <TextBlock x:Name="txtIntroduction" Style="{StaticResource InformationDocumentTitleStyle}"
                 FontFamily="segoe ui" LineHeight="16" Margin="10,10,10,10" FontSize="12" TextWrapping="Wrap"/>
    </StackPanel>
    <TextBox x:Name="txtMessage" Grid.Row="1" Margin="10" Background="{DynamicResource WhiteBrush}"
             FontSize="12" AcceptsReturn="true" AcceptsTab="true" TextWrapping="Wrap"
             VerticalScrollBarVisibility="Auto"/>
    <Border Grid.Row="2">
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button Click="Button_Click" Content="OK" IsCancel="true" MinWidth="75" Margin="10,0,10,10"/>
      </StackPanel>
    </Border>
  </Grid>
</controls:MetroWindow>