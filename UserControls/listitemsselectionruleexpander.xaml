<UserControl x:Class="Builder.Presentation.UserControls.ListItemsSelectionRuleExpander"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             xmlns:controls="clr-namespace:Builder.Presentation.Controls"
             xmlns:viewModels="clr-namespace:Builder.Presentation.ViewModels"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:controls1="clr-namespace:Aurora.Presentation.Controls;assembly=Aurora.Presentation">
  <FrameworkElement.Resources>
    <Style x:Key="HeaderTextBlockStyle" TargetType="{x:Type TextBlock}">
      <Setter Property="FrameworkElement.VerticalAlignment" Value="Center"/>
      <Setter Property="TextBlock.FontWeight" Value="SemiBold"/>
      <Setter Property="TextBlock.Foreground" Value="{DynamicResource IdealForegroundColorBrush}"/>
      <Setter Property="TextBlock.Text" Value="{Binding Converter={StaticResource UpperCaseConverter}}"/>
    </Style>
    <DataTemplate x:Key="ExpanderHeaderTemplate">
      <TextBlock Margin="3,2,0,2" Style="{StaticResource HeaderTextBlockStyle}"/>
    </DataTemplate>
    <Style x:Key="SelectedElementTextBlockStyle" TargetType="{x:Type TextBlock}">
      <Setter Property="FrameworkElement.VerticalAlignment" Value="Center"/>
      <Setter Property="TextBlock.FontWeight" Value="SemiBold"/>
      <Setter Property="TextBlock.TextWrapping" Value="WrapWithOverflow"/>
      <Setter Property="TextBlock.Foreground" Value="{DynamicResource IdealForegroundColorBrush}"/>
    </Style>
    <Style x:Key="MuiExpander" TargetType="{x:Type Expander}">
      <Setter Property="Control.Foreground" Value="{DynamicResource ItemText}"/>
    </Style>
    <ControlTemplate x:Key="AnimatedExpanderTemplate" TargetType="{x:Type ToggleButton}">
      <Grid HorizontalAlignment="Stretch" Height="20"
            Width="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=ActualWidth}">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="50*"/>
          <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Rectangle Grid.ColumnSpan="3" Fill="Transparent" StrokeThickness="0"/>
        <Path x:Name="Arrow" Grid.Column="0" Fill="{DynamicResource IdealForegroundColorBrush}" Width="10"
              Height="8" Margin="5" Stretch="Fill"
              Data="F1 M 37.8516,39.5833 L 52.1016,24.9375 52.1016,35.2292 37.8516,50.2708 23.6016,35.2292 23.6016,24.9375 37.8516,39.5833 Z"
              HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5">
          <UIElement.RenderTransform>
            <RotateTransform Angle="0"/>
          </UIElement.RenderTransform>
        </Path>
        <ContentPresenter Grid.Column="1" VerticalAlignment="Center" Margin="5,0,0,0"/>
      </Grid>
      <ControlTemplate.Triggers>
        <Trigger Property="ToggleButton.IsChecked" Value="True">
          <TriggerBase.EnterActions>
            <BeginStoryboard>
              <Storyboard>
                <DoubleAnimation Storyboard.TargetName="Arrow"
                                 Storyboard.TargetProperty="(Path.RenderTransform).(RotateTransform.Angle)" To="180"
                                 Duration="0:0:0.2"/>
              </Storyboard>
            </BeginStoryboard>
          </TriggerBase.EnterActions>
          <TriggerBase.ExitActions>
            <BeginStoryboard>
              <Storyboard>
                <DoubleAnimation Storyboard.TargetName="Arrow"
                                 Storyboard.TargetProperty="(Path.RenderTransform).(RotateTransform.Angle)" To="0"
                                 Duration="0:0:0.2"/>
              </Storyboard>
            </BeginStoryboard>
          </TriggerBase.ExitActions>
        </Trigger>
      </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style TargetType="{x:Type Expander}">
      <Setter Property="Control.Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type Expander}">
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition x:Name="ContentRow" Height="0"/>
              </Grid.RowDefinitions>
              <Border Name="Border" Background="{DynamicResource HighlightBrush}"
                      BorderBrush="{StaticResource AccentColorBrush}" BorderThickness="0,0,0,0" CornerRadius="0"
                      Grid.Row="0">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                  </Grid.ColumnDefinitions>
                  <ToggleButton Template="{StaticResource AnimatedExpanderTemplate}" Margin="0" HorizontalAlignment="Right"
                                OverridesDefaultStyle="true" Grid.Column="2"
                                IsChecked="{Binding Path=IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                    <FrameworkElement.ContextMenu>
                      <ContextMenu>
                        <MenuItem Header="SelectionRule Info"/>
                      </ContextMenu>
                    </FrameworkElement.ContextMenu>
                  </ToggleButton>
                  <ContentPresenter Grid.Column="0" Margin="2,0" ContentSource="Header" RecognizesAccessKey="true"/>
                </Grid>
              </Border>
              <Border Name="Content" Opacity="1" BorderBrush="{StaticResource BlackBrush}" BorderThickness="0"
                      CornerRadius="0,0,0,0" Grid.Row="1">
                <ContentPresenter Margin="0"/>
              </Border>
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="Expander.IsExpanded" Value="true">
                <Setter TargetName="ContentRow" Value="{Binding ElementName=Content, Path=Height}"
                        Property="RowDefinition.Height"/>
                <Setter TargetName="Border" Value="{StaticResource WhiteBrush}" Property="Border.BorderBrush"/>
              </Trigger>
              <Trigger Property="Expander.IsExpanded" Value="false">
                <Setter TargetName="Border" Property="Border.BorderThickness" Value="0,0,0,0"/>
                <Setter TargetName="Border" Property="Border.CornerRadius" Value="0"/>
                <Setter TargetName="Content" Property="UIElement.Visibility" Value="Collapsed"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </FrameworkElement.Resources>
  <Grid Margin="0,0,0,0">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <Border BorderThickness="0" Width="4" BorderBrush="{DynamicResource GrayBrush1}" Margin="0,0,10,0"
            Background="{DynamicResource PinnedCharacterIconColorBrush}" SnapsToDevicePixels="true"/>
    <Border x:Name="VisualFocusQue" BorderThickness="0" Width="4" BorderBrush="{DynamicResource GrayBrush1}"
            Margin="0,0,10,0" Background="{DynamicResource HighlightBrush}" SnapsToDevicePixels="true">
      <FrameworkElement.Triggers>
        <EventTrigger RoutedEvent="Border.RequestBringIntoView">
          <BeginStoryboard>
            <Storyboard>
              <DoubleAnimation From="1" To="0" AutoReverse="true" Duration="0:0:0.75" Storyboard.TargetName="VisualFocusQue"
                               Storyboard.TargetProperty="Opacity" RepeatBehavior="3x"/>
            </Storyboard>
          </BeginStoryboard>
        </EventTrigger>
      </FrameworkElement.Triggers>
    </Border>
    <Border Grid.Column="1" Margin="0,0,0,15">
      <controls:AuroraExpander x:Name="Expander" Padding="0" Header="{Binding Header}" IsExpanded="{Binding IsExpanded}"
                               FooterVisibility="{Binding SelectionMade, Converter={StaticResource BooleanToVisibilityConverter}}">
        <Grid Background="{DynamicResource WhiteBrush}">
          <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          <FrameworkElement.Resources>
            <Style TargetType="{x:Type DataGridColumnHeader}" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
              <Setter Property="Control.Background" Value="{DynamicResource WindowBorder}"/>
            </Style>
          </FrameworkElement.Resources>
          <DataGrid SelectionChanged="ElementsGridSelectionChanged" KeyDown="ElementsGridKeyDown"
                    MouseDoubleClick="___No_Name__MouseDoubleClick" x:Name="DataGrid" Grid.Row="0" Grid.RowSpan="2"
                    Grid.Column="0" IsReadOnly="true" AutoGenerateColumns="false" HeadersVisibility="None"
                    BorderThickness="0" SelectionMode="Single" MinRowHeight="21" CanUserReorderColumns="false"
                    ItemsSource="{Binding SelectionItems}"
                    SelectedItem="{Binding SelectedItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <DataGrid.Columns>
              <DataGridTextColumn Header="NAME" Width="*" Binding="{Binding Name}">
                <DataGridColumn.CellStyle>
                  <Style TargetType="{x:Type DataGridCell}" BasedOn="{StaticResource {x:Type DataGridCell}}">
                    <Setter Property="Control.Background" Value="{DynamicResource WhiteBrush}"/>
                    <Setter Property="Control.Foreground" Value="{DynamicResource BlackBrush}"/>
                    <Setter Property="Control.BorderThickness" Value="0"/>
                    <Setter Property="Control.Padding" Value="5,0"/>
                    <Setter Property="ContentControl.ContentTemplate">
                      <Setter.Value>
                        <DataTemplate>
                          <TextBlock Margin="0,0,0,0" VerticalAlignment="Center" Text="{Binding Path=Text}"/>
                        </DataTemplate>
                      </Setter.Value>
                    </Setter>
                    <Setter Property="Control.Template">
                      <Setter.Value>
                        <ControlTemplate TargetType="{x:Type DataGridCell}">
                          <Border Background="{TemplateBinding Control.Background}"
                                  BorderBrush="{TemplateBinding Control.BorderBrush}"
                                  BorderThickness="{TemplateBinding Control.BorderThickness}"
                                  Padding="{TemplateBinding Control.Padding}" SnapsToDevicePixels="true">
                            <ContentPresenter SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"/>
                          </Border>
                        </ControlTemplate>
                      </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                      <Trigger Property="DataGridCell.IsSelected" Value="true">
                        <Setter Property="Control.Background" Value="{DynamicResource AccentColorBrush3}"/>
                        <Setter Property="Control.Foreground" Value="{DynamicResource IdealForegroundColorBrush}"/>
                      </Trigger>
                    </Style.Triggers>
                  </Style>
                </DataGridColumn.CellStyle>
              </DataGridTextColumn>
            </DataGrid.Columns>
          </DataGrid>
        </Grid>
        <controls:AuroraExpander.FooterContent>
          <Grid Height="24">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto"/>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="Auto"/>
              <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <FrameworkElement.Resources>
              <Style x:Key="SelectedElementTextBlockStyle" TargetType="{x:Type TextBlock}">
                <Setter Property="FrameworkElement.VerticalAlignment" Value="Center"/>
                <Setter Property="TextBlock.FontSize" Value="11"/>
                <Setter Property="FrameworkElement.Margin" Value="4,0"/>
                <Setter Property="TextBlock.FontWeight" Value="SemiBold"/>
                <Setter Property="TextBlock.TextWrapping" Value="WrapWithOverflow"/>
                <Setter Property="TextBlock.Foreground" Value="{DynamicResource DarkColor1Brush}"/>
              </Style>
            </FrameworkElement.Resources>
            <iconPacks:PackIconMaterial Kind="CheckboxMarkedOutline" Margin="6,0,4,0" Foreground="{DynamicResource SuccessDarkColorBrush}"/>
            <TextBlock Style="{StaticResource SelectedElementTextBlockStyle}" Grid.Column="1"
                       Text="{Binding RegisteredItem.Text}"/>
            <controls1:CommandButton Click="UnsetElement" ToolTip="Deselect the current selection." Grid.Column="3" Margin="4,0"
                                     Style="{StaticResource DangerIconCommandButtonStyle}">
              <iconPacks:PackIconMaterial Kind="Close" Height="10" Width="10"/>
            </controls1:CommandButton>
          </Grid>
        </controls:AuroraExpander.FooterContent>
      </controls:AuroraExpander>
    </Border>
  </Grid>
</UserControl>