<UserControl x:Class="Builder.Presentation.UserControls.ConditionsPanelContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:Builder.Presentation.Converters"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             xmlns:viewModels="clr-namespace:Builder.Presentation.ViewModels"
             xmlns:content="clr-namespace:Builder.Presentation.ViewModels.Content;assembly=Aurora.Logic"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls">
  <Grid Margin="0,0,0,0">
    <FrameworkElement.DataContext>
      <content:ConditionsPanelContentViewModel/>
    </FrameworkElement.DataContext>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <StackPanel Margin="0,0,0,1"
                Visibility="{Binding HasConditions, Converter={StaticResource BooleanToVisibilityConverter}}">
      <TextBlock TextWrapping="Wrap" FontSize="11" FontWeight="Normal" FontStyle="Italic" Margin="5,3"
                 Text="{Binding DisplayResistances}"/>
    </StackPanel>
  </Grid>
</UserControl>