name: Build WPF (Aurora.Legacy)

on:
  push:
    paths:
      - 'Aurora.Lights/**'
      - 'Aurora.Logic/**'
      - 'Builder.Core/**'
      - 'Builder.Data/**'
      - 'Aurora.Documents/**'
      - 'Aurora.Presentation/**'
      - 'lib/**'
      - '.github/workflows/build-wpf.yml'
  workflow_dispatch:

jobs:
  build-wpf:
    runs-on: windows-latest
    env:
      PROJECT: Aurora.Lights/Aurora.Legacy.csproj

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        shell: bash
        run: dotnet restore "$PROJECT"

      - name: Build
        shell: bash
        run: |
          dotnet build "$PROJECT" \
            -c Release \
            --no-restore \
            -o publish/wpf

      - name: List publish output
        shell: bash
        run: ls -la publish/wpf

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: aurora-legacy-wpf
          path: publish/wpf
